#pragma kernel CSTEST

RWStructuredBuffer<uint4> permProps[32];

[numthreads(32, 1, 1)]
void CSTEST(uint3 id : SV_DispatchThreadID)
{
	for (int i = 0; i < 32; i++)
	{
		permProps[0][id.x] = uint4(0, 0, 0, 0);
	}
}